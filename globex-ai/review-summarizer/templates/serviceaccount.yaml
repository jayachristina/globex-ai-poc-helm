{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "review-summarizer.serviceAccountName" . }}
  labels:
    {{- include "review-summarizer.labels" . | nindent 4 }}
  annotations:
    {{- include "review-summarizer.argocd-syncwave" . | nindent 4 }}
{{- end }}
