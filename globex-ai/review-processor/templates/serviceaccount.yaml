{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "review-processor.serviceAccountName" . }}
  labels:
    {{- include "review-processor.labels" . | nindent 4 }}
  annotations:
    {{- include "review-processor.argocd-syncwave" . | nindent 4 }}
{{- end }}
